# 1. Ignore everything in the root
/*


# 2. Allow all files in the root directory (like README.md, main.py, etc.)
!/*.*

# 3. Allow the .gitignore file itself
!.gitignore

# 4. Allow the src folder and everything inside it
!/src/

# Don't track log files to keep repository clean
logs.log
*.log